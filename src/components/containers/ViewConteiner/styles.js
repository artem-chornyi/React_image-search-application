const styles = theme => ({
    viewContainer: {
        background: '#001935',
        width: '100%',
        minHeight: '100vh'
    },

    wrapper: {
        maxWidth: 1300,
        padding: 30,
        margin: 'auto',
    }
})

export default styles;
